"use client";

import CardLayout from "@/components/cardLayout";

export default function StaticsDetail() {
  return (
    <div className="mb-6">
      <CardLayout leftContent={<p>No disponible.</p>} />
    </div>
  );
}
